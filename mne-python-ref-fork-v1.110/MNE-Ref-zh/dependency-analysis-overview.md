# MNE-Python 依赖库分析总览

> **创建日期**: 2026-01-30  
> **目的**: 全面分析 MNE-Python 对 NumPy、SciPy、scikit-learn 及其他关键库的依赖关系

---

## 目录

1. [依赖架构图](#依赖架构图)
2. [核心依赖概览](#核心依赖概览)
3. [三大科学计算库](#三大科学计算库)
4. [完整依赖树](#完整依赖树)
5. [文档导航](#文档导航)

---

## 依赖架构图

```
MNE-Python 核心架构
        │
        ├── 必需依赖 (Core Dependencies)
        │   ├── NumPy >= 1.26        ← 数组运算、线性代数、FFT
        │   ├── SciPy >= 1.11        ← 信号处理、稀疏矩阵、优化
        │   ├── Matplotlib >= 3.8    ← 可视化
        │   ├── Decorator            ← 装饰器工具
        │   ├── Jinja2               ← 模板引擎
        │   ├── Lazy_loader >= 0.3   ← 延迟加载
        │   ├── Packaging            ← 版本管理
        │   ├── Pooch >= 1.5         ← 数据下载
        │   └── Tqdm                 ← 进度条
        │
        ├── 可选依赖 (Optional - Full Installation)
        │   ├── scikit-learn >= 1.3  ← 机器学习、解码分析
        │   ├── Pandas >= 2.1        ← 数据表格操作
        │   ├── Nibabel              ← MRI 数据读取
        │   ├── Nilearn              ← 神经影像分析
        │   ├── PyVista >= 0.43      ← 3D 可视化
        │   ├── Joblib               ← 并行计算
        │   ├── H5py                 ← HDF5 文件格式
        │   ├── Numba                ← JIT 编译加速
        │   ├── Dipy                 ← 扩散成像
        │   └── OpenMEEG >= 2.5.7    ← BEM 计算
        │
        └── 开发依赖 (Development)
            ├── Pytest >= 8.0        ← 测试框架
            ├── Sphinx >= 6          ← 文档生成
            ├── Numpydoc             ← NumPy 风格文档
            ├── Ruff                 ← 代码检查
            ├── Mypy                 ← 类型检查
            └── Pre-commit           ← Git hooks
```

---

## 核心依赖概览

### 1. 必需依赖 (9 个)

| 依赖库 | 版本要求 | 主要用途 | MNE 中的角色 |
|--------|---------|---------|-------------|
| **NumPy** | >= 1.26, < 3 | 数组计算 | 数据存储、数学运算、FFT |
| **SciPy** | >= 1.11 | 科学计算 | 信号处理、稀疏矩阵、优化 |
| **Matplotlib** | >= 3.8 | 绘图 | 时间序列图、地形图、3D 图 |
| Decorator | 任意 | 装饰器 | 函数装饰工具 |
| Jinja2 | 任意 | 模板 | HTML 报告生成 |
| Lazy_loader | >= 0.3 | 延迟加载 | 模块延迟导入 |
| Packaging | 任意 | 版本管理 | 依赖版本检查 |
| Pooch | >= 1.5 | 数据管理 | 示例数据下载 |
| Tqdm | 任意 | 进度条 | 长时间运算反馈 |

---

### 2. 可选依赖 (Full Installation: 25+ 个)

#### 2.1 机器学习与解码

| 依赖库 | 版本要求 | 功能模块 | 用途 |
|--------|---------|---------|------|
| **scikit-learn** | >= 1.3 | `mne.decoding` | 分类、回归、降维、交叉验证 |
| Joblib | 任意 | 并行计算 | 多核加速 |

#### 2.2 神经影像支持

| 依赖库 | 版本要求 | 功能模块 | 用途 |
|--------|---------|---------|------|
| Nibabel | 任意 | MRI 读取 | NIfTI/MGH 文件 |
| Nilearn | 任意 | 脑影像 | fMRI 分析集成 |
| Dipy | 任意 | 扩散成像 | dMRI 数据处理 |

#### 2.3 可视化

| 依赖库 | 版本要求 | 功能模块 | 用途 |
|--------|---------|---------|------|
| PyVista | >= 0.43 | 3D 渲染 | 大脑表面、源定位可视化 |
| PyVistaqt | >= 0.11 | GUI 集成 | Qt 嵌入 3D 窗口 |
| QtPy | 任意 | Qt 抽象层 | PyQt/PySide 兼容 |

#### 2.4 数据格式

| 依赖库 | 版本要求 | 功能模块 | 用途 |
|--------|---------|---------|------|
| H5py | 任意 | HDF5 | .hdf5/.h5 文件读写 |
| Pandas | >= 2.1 | 表格数据 | DataFrame 导出 |
| PyArrow | 任意 | Pandas 后端 | 避免 Pandas 警告 |

#### 2.5 性能优化

| 依赖库 | 版本要求 | 功能模块 | 用途 |
|--------|---------|---------|------|
| Numba | 任意 | JIT 编译 | 加速数值计算 |
| OpenMEEG | >= 2.5.7 | BEM 求解器 | 精确正向模型 |

---

### 3. 开发与测试依赖 (15+ 个)

| 依赖库 | 用途 |
|--------|------|
| Pytest | 单元测试、集成测试 |
| Pytest-cov | 代码覆盖率 |
| Pytest-timeout | 测试超时控制 |
| Pytest-rerunfailures | 失败重试 |
| Sphinx | 文档生成 |
| Sphinx-gallery | 示例画廊 |
| Numpydoc | NumPy 风格文档字符串 |
| Ruff | 快速 Linting |
| Mypy | 静态类型检查 |
| Vulture | 死代码检测 |
| Codespell | 拼写检查 |

---

## 三大科学计算库

### NumPy - 数据基础架构

**依赖声明**: `numpy >= 1.26, < 3`

**使用频率**: 🔥🔥🔥🔥🔥 (100% 核心文件)

**主要功能域**:
1. **数据存储** - 所有 EEG/MEG 数据以 NumPy 数组形式存储
2. **线性代数** - `np.linalg`, `np.dot`, `np.einsum`
3. **FFT 变换** - `np.fft` (信号分析、滤波)
4. **数学函数** - 三角函数、对数、指数
5. **测试工具** - `numpy.testing` (单元测试断言)

**详细分析**: 见 [dependency-numpy.md](dependency-numpy.md)

---

### SciPy - 科学计算引擎

**依赖声明**: `scipy >= 1.11`

**使用频率**: 🔥🔥🔥🔥🔥 (核心算法依赖)

**主要模块**:
1. **scipy.signal** - 滤波器设计、重采样、窗函数
2. **scipy.sparse** - 稀疏矩阵 (邻接矩阵、正向模型)
3. **scipy.linalg** - 线性代数 (SVD、特征值分解)
4. **scipy.stats** - 统计检验 (t-test, F-test, permutation)
5. **scipy.spatial** - 空间计算 (Delaunay, KDTree, 距离)
6. **scipy.optimize** - 优化算法 (最小二乘、约束优化)
7. **scipy.interpolate** - 插值 (通道插值、时频插值)
8. **scipy.ndimage** - 图像处理 (形态学操作)
9. **scipy.fft** - 快速傅里叶变换

**详细分析**: 见 [dependency-scipy.md](dependency-scipy.md)

---

### scikit-learn - 机器学习工具箱

**依赖声明**: `scikit-learn >= 1.3` (可选依赖)

**使用频率**: 🔥🔥🔥 (解码分析模块)

**主要应用**:
1. **分类器** - SVM, LogisticRegression, LDA
2. **回归** - Ridge, LinearRegression, KernelRidge
3. **降维** - PCA, ICA (FastICA)
4. **交叉验证** - KFold, StratifiedKFold, cross_val_score
5. **特征提取** - grid_to_graph (空间邻接)
6. **模型评估** - scorer, metrics
7. **Pipeline** - 组合数据流
8. **Transformer API** - MNE 遵循 sklearn 标准

**详细分析**: 见 [dependency-sklearn.md](dependency-sklearn.md)

---

## 完整依赖树

### 依赖级别分类

```
Level 0: Python 标准库
  ├── os, sys, pathlib
  ├── copy, collections
  ├── math, functools
  └── warnings, logging

Level 1: 基础科学计算 (必需)
  ├── NumPy >= 1.26
  ├── SciPy >= 1.11
  └── Matplotlib >= 3.8

Level 2: 工具库 (必需)
  ├── Decorator
  ├── Jinja2
  ├── Lazy_loader >= 0.3
  ├── Packaging
  ├── Pooch >= 1.5
  └── Tqdm

Level 3: 扩展功能 (可选)
  ├── scikit-learn >= 1.3
  ├── Pandas >= 2.1
  ├── PyVista >= 0.43
  ├── Nibabel
  ├── Nilearn
  ├── H5py
  ├── Joblib
  └── Numba

Level 4: GUI/可视化 (可选)
  ├── PyQt6 / PySide6
  ├── PyVistaqt >= 0.11
  ├── QtPy
  ├── IPython
  ├── Jupyter
  └── ipywidgets

Level 5: 特定数据格式 (可选)
  ├── Neo (多种电生理格式)
  ├── SNIRF (fNIRS)
  ├── Pybv (BrainVision)
  ├── EDFIO >= 0.4.10 (EDF)
  ├── Eeglabio (EEGLAB)
  └── Mffpy >= 0.5.7 (EGI MFF)

Level 6: 专业计算 (可选)
  ├── OpenMEEG >= 2.5.7 (BEM)
  ├── Dipy (扩散成像)
  ├── Python-picard (ICA)
  └── Numba (加速)
```

---

## 文档导航

### 详细分析文档

1. **[NumPy 依赖分析](dependency-numpy.md)**
   - 数组操作模式
   - 线性代数使用
   - FFT 应用场景
   - 测试工具集成

2. **[SciPy 依赖分析](dependency-scipy.md)**
   - signal 模块详解
   - sparse 稀疏矩阵应用
   - linalg 线性代数
   - stats 统计检验
   - spatial 空间计算
   - optimize 优化算法
   - interpolate 插值应用
   - ndimage 图像处理

3. **[scikit-learn 依赖分析](dependency-sklearn.md)**
   - 解码模块架构
   - 分类器集成
   - Pipeline 使用模式
   - 交叉验证策略
   - Transformer 设计

4. **[其他重要依赖](dependency-others.md)**
   - Matplotlib 可视化
   - PyVista 3D 渲染
   - Pandas 数据操作
   - Nibabel MRI 支持
   - Joblib 并行计算

---

## 依赖关系逻辑

### 数据流与依赖

```
数据读取 → NumPy 数组存储
    ↓
信号处理 → SciPy.signal (滤波、重采样)
    ↓
特征提取 → NumPy (数学运算)
    ↓
降维/分类 → scikit-learn (PCA, LDA, SVM)
    ↓
统计检验 → SciPy.stats (t-test, permutation)
    ↓
可视化 → Matplotlib / PyVista
```

### 核心算法链

```
源定位流程:
    Raw/Epochs (NumPy 数组)
        → Forward Solution (SciPy.sparse 稀疏矩阵)
        → Covariance (NumPy.linalg 协方差)
        → Inverse Operator (SciPy.linalg SVD)
        → Source Estimate (NumPy 数组)

解码分析流程:
    Epochs (NumPy 数组)
        → Scaler (sklearn.preprocessing)
        → Spatial Filter (CSP, NumPy.linalg)
        → Classifier (sklearn.svm.SVC)
        → Cross-validation (sklearn.model_selection)
        → Metrics (sklearn.metrics)

统计分析流程:
    Data (NumPy 数组)
        → Adjacency Matrix (SciPy.sparse)
        → Cluster Formation (SciPy.ndimage)
        → Permutation Test (NumPy.random + SciPy.stats)
        → Multiple Comparison Correction (SciPy.stats)
```

---

## 版本策略

### 最低版本策略

MNE-Python 采用 **SPEC 0** 策略 (Scientific Python Ecosystem Coordination):

```
发布时间 + 2.5 年 → 最低支持版本

示例:
- NumPy 1.26 发布于 2023-09-16
  → 最低版本在 2026-06-16 升级为 2.0
  
- SciPy 1.11 发布于 2023-06-28
  → 最低版本在 2026-01-19 升级为 1.12
  
- scikit-learn 1.3 发布于 2023-06-30
  → 最低版本在 2026-01-17 升级为 1.4
```

**优势**:
- 保持与科学生态系统同步
- 利用新特性和性能改进
- 避免维护旧版本兼容性

---

## 性能影响分析

### NumPy 性能关键点

| 操作 | NumPy 函数 | 性能影响 | 优化方法 |
|------|-----------|---------|---------|
| 矩阵乘法 | `np.dot`, `np.matmul` | 高 | BLAS 优化 |
| FFT | `np.fft.rfft` | 高 | FFTW 后端 |
| 数组广播 | 自动广播 | 中 | 预分配内存 |
| 索引切片 | `arr[idx]` | 低 | 视图优化 |

### SciPy 性能关键点

| 操作 | SciPy 函数 | 性能影响 | 优化方法 |
|------|-----------|---------|---------|
| 稀疏矩阵乘法 | `sparse.csr_array` @ | 高 | CSR 格式 |
| SVD | `linalg.svd` | 高 | LAPACK 优化 |
| IIR 滤波 | `signal.filtfilt` | 中 | 预计算系数 |
| 优化求解 | `optimize.leastsq` | 高 | 提供雅可比矩阵 |

### scikit-learn 性能关键点

| 操作 | sklearn 函数 | 性能影响 | 优化方法 |
|------|-------------|---------|---------|
| PCA | `decomposition.PCA` | 中 | 增量 PCA |
| SVM | `svm.SVC` | 高 | 调整 C 和 gamma |
| 交叉验证 | `cross_val_score` | 高 | n_jobs=-1 并行 |
| Pipeline | `pipeline.Pipeline` | 低 | 缓存中间结果 |

---

## 替代方案与兼容性

### NumPy 替代

- **JAX**: Google 的自动微分和 GPU 加速
  - MNE 暂不支持，未来可能集成
- **CuPy**: NVIDIA GPU 加速 NumPy
  - MNE 通过 CUDA 模块部分支持

### SciPy 替代

- **PyTorch**: 深度学习框架
  - 可用于神经网络解码，需手动转换
- **TensorFlow**: Google 深度学习框架
  - 类似 PyTorch，间接支持

### scikit-learn 替代

- **XGBoost / LightGBM**: 梯度提升
  - 可通过 sklearn 接口使用
- **PyTorch / TensorFlow**: 深度学习
  - 需自定义 MNE → Tensor 转换

---

## 常见问题

### Q1: 为什么 NumPy 是必需的？

A: NumPy 提供 N 维数组数据结构，是所有科学计算库的基础。MNE 所有数据 (Raw, Epochs, Evoked) 内部都是 NumPy 数组。

### Q2: SciPy 和 NumPy 的 linalg 模块有什么区别？

A: 
- `numpy.linalg`: 基础线性代数，适用于稠密数组
- `scipy.linalg`: 扩展功能，包括 SVD、特征值分解、稀疏矩阵支持

MNE 优先使用 `scipy.linalg` 因为其功能更强大。

### Q3: scikit-learn 是必需的吗？

A: 不是必需，但如果使用 `mne.decoding` 模块进行分类/回归分析，则必须安装。

### Q4: 如何检查我的 MNE 安装缺少哪些依赖？

```python
import mne
mne.sys_info()  # 显示所有依赖及其版本
```

### Q5: 如何安装完整依赖？

```bash
# 方法 1: pip
pip install mne[full]

# 方法 2: conda
conda install -c conda-forge mne-base mne-qt-browser pyvista pyvistaqt scikit-learn
```

---

## 更新历史

| 日期 | 变更 | 影响 |
|------|------|------|
| 2026-01-30 | 创建文档 | 初始版本 |

---

**下一步**: 
- 查看 [NumPy 详细分析](dependency-numpy.md)
- 查看 [SciPy 详细分析](dependency-scipy.md)
- 查看 [scikit-learn 详细分析](dependency-sklearn.md)
