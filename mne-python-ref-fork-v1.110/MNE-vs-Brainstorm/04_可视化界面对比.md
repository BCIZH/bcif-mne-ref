# 可视化与用户界面对比: MNE-Python vs Brainstorm3

> **专题**: 数据可视化与交互界面设计对比  
> **更新**: 2026年1月31日

---

## 目录

1. [设计哲学](#设计哲学)
2. [数据可视化](#数据可视化)
3. [交互界面](#交互界面)
4. [3D大脑可视化](#3d大脑可视化)
5. [报告生成](#报告生成)

---

## 设计哲学

### MNE-Python: 编程+交互混合

**核心理念**: "脚本生成图形,图形支持交互"

```python
# 编程创建图形
import mne
raw = mne.io.read_raw_fif('data.fif')
fig = raw.plot(duration=10, n_channels=30)

# 图形窗口支持交互:
# - 鼠标拖动: 平移时间轴
# - 滚轮: 缩放
# - 点击通道: 标记为坏通道
# - 键盘快捷键: b(标记坏段), a(标注)

# 所有操作可通过代码实现
raw.info['bads'] = ['MEG 0113']
raw.annotations.append(onset=10.5, duration=1.0, description='BAD_noise')
```

**优势**:
- ✅ 可重复 (代码即记录)
- ✅ 批量生成 (循环创建图形)
- ✅ 自定义 (完全控制参数)

**劣势**:
- ❌ 学习曲线陡峭 (需要编程)
- ❌ 非程序员不友好

---

### Brainstorm3: GUI优先

**核心理念**: "点击即可视化,操作即分析"

```
GUI工作流:
1. 双击数据文件 → 自动打开可视化窗口
2. 右键菜单 → 显示所有可能操作
3. 拖拽滑块 → 实时更新图形
4. 点击脑区 → 显示时间序列
5. 所有操作GUI完成,无需编程
```

**优势**:
- ✅ 零编程 (完全GUI操作)
- ✅ 实时反馈 (操作立即生效)
- ✅ 探索友好 (快速试错)

**劣势**:
- ❌ 可重复性差 (需手动记录步骤)
- ❌ 批量处理困难

---

## 数据可视化

### 1. 原始数据可视化

#### MNE-Python: raw.plot()

```python
import mne

raw = mne.io.read_raw_fif('data.fif', preload=True)

# 基本绘图
fig = raw.plot(
    duration=10,        # 显示10秒
    n_channels=30,      # 显示30个通道
    scalings='auto',    # 自动缩放
    block=False         # 非阻塞模式
)

# 高级参数
fig = raw.plot(
    duration=10,
    n_channels=30,
    bgcolor='white',    # 背景色
    color={'meg': 'blue', 'eeg': 'red'},  # 通道颜色
    bad_color='red',    # 坏通道颜色
    event_color='cyan', # 事件颜色
    scalings=dict(mag=1e-12, grad=4e-11, eeg=20e-6),  # 自定义缩放
    remove_dc=True,     # 移除DC偏移
    order='selection',  # 通道排序
    show_options=True,  # 显示选项面板
    title='Raw Data',
    show_scrollbars=True,
    show_scalebars=True,
    clipping='transparent'  # 截断处理
)

# 交互功能:
# - 空格: 标记坏段
# - b: 标记坏通道
# - a: 添加标注
# - 鼠标拖动: 平移
# - 滚轮: 缩放
# - 左键拖动: 选择区域
```

**可视化后端**:
- Matplotlib (默认,静态+有限交互)
- PyQt/PySide (完整GUI)
- notebook (Jupyter集成)

---

#### Brainstorm3: 时间序列查看器

```matlab
% 自动打开 (双击数据文件)
% 或脚本调用
view_timeseries(DataFile, 'MEG');

% GUI功能:
% - 滑块: 时间导航
% - 缩放工具: 放大/缩小
% - 通道选择: 多选/取消
% - 事件编辑: 右键添加/删除
% - 滤波预览: 实时应用滤波器
% - 拓扑图: 点击时间点显示拓扑
% - 频谱: 点击通道显示PSD
```

**特色功能**:
- ✅ **联动视图**: 时间序列+拓扑图+频谱同时显示
- ✅ **实时滤波**: 滑块调整截止频率,立即看到效果
- ✅ **事件检测**: 自动检测心跳/眨眼,可视化标记

---

### 2. Epochs 可视化

#### MNE-Python

```python
epochs = mne.Epochs(raw, events, event_id, tmin=-0.2, tmax=0.5)

# 1. 所有试次
epochs.plot(
    picks='eeg',
    n_epochs=20,        # 同时显示20个试次
    n_channels=30,
    scalings='auto',
    block=False
)

# 交互功能:
# - 点击试次: 标记为坏试次
# - 左右箭头: 翻页
# - +/-: 调整缩放

# 2. 拓扑图序列
epochs.plot_image(
    picks='eeg',
    combine='mean',     # 或 'gfp' (全局场功率)
    sigma=1.0,          # 平滑参数
    vmin=-100, vmax=100
)

# 3. 蝶形图 + 拓扑图
epochs.plot_topo_image()

# 4. 单试次详情
epochs.plot(picks=[0, 1, 2], events=events)
```

---

#### Brainstorm3

```matlab
% 双击trials文件
view_data(TrialsFile);

% GUI功能:
% - 试次导航: 上一个/下一个
% - 叠加显示: 所有试次透明叠加
% - 平均更新: 实时计算平均
% - 坏试次标记: 右键菜单
% - 拓扑动画: 播放ERF演化
```

---

### 3. Evoked 可视化

#### MNE-Python: 丰富的绘图类型

```python
evoked = epochs.average()

# 1. 蝴蝶图 (Butterfly plot)
evoked.plot(
    gfp=True,           # 显示全局场功率
    spatial_colors=True, # 空间颜色编码
    zorder='std',       # 标准差排序
    selectable=True     # 可点击选择通道
)

# 2. 拓扑图 (Topomap)
evoked.plot_topomap(
    times=[0, 0.1, 0.2, 0.3],  # 多个时间点
    ch_type='eeg',
    contours=6,                 # 等高线数量
    sensors=True,               # 显示传感器位置
    colorbar=True,
    size=3,                     # 图形大小
    cmap='RdBu_r'               # 色图
)

# 3. 联合图 (Joint plot)
evoked.plot_joint(
    times=[0.1, 0.2],           # 关键时间点
    title='ERP Components'
)

# 4. 图像图 (Image)
evoked.plot_image(
    picks='eeg',
    xlim=[0, 0.5],
    clim=dict(eeg=[-10, 10])
)

# 5. 白色背景 (发表级)
evoked.plot_white(
    noise_cov=noise_cov,        # 噪声归一化
    show=True
)

# 6. 3D拓扑图
evoked.plot_topomap_3d(
    ch_type='eeg',
    subject='fsaverage',
    subjects_dir=subjects_dir
)
```

**发表级图形**:
```python
import matplotlib.pyplot as plt

fig, axes = plt.subplots(2, 2, figsize=(12, 10))

# 子图1: 蝴蝶图
evoked.plot(axes=axes[0, 0], show=False)
axes[0, 0].set_title('Butterfly Plot')

# 子图2: 拓扑图
evoked.plot_topomap(times=[0.1], axes=axes[0, 1], show=False)

# 子图3: 图像图
evoked.plot_image(axes=axes[1, 0], show=False)

# 子图4: GFP
evoked.plot_gfp(axes=axes[1, 1], show=False)

plt.tight_layout()
plt.savefig('figure1.png', dpi=300)
```

---

#### Brainstorm3: 集成可视化

```matlab
% 双击平均文件
view_data(AverageFile);

% 自动显示:
% - 时间序列 (上)
% - 拓扑图动画 (下左)
% - 3D传感器阵列 (下右)

% 交互功能:
% - 拖动时间滑块 → 拓扑图实时更新
% - 点击拓扑图位置 → 高亮对应通道
% - 双击通道 → 单独放大显示
% - 右键 → 导出高分辨率图像
```

**特色**: 三联图自动同步,无需编程

---

### 4. 源空间可视化

#### MNE-Python: 3D交互式大脑

```python
stc = apply_inverse(evoked, inv, lambda2=1/9, method='dSPM')

# 1. 基本3D可视化
brain = stc.plot(
    subjects_dir=subjects_dir,
    subject='fsaverage',
    hemi='split',       # 'lh' | 'rh' | 'both' | 'split'
    views='lateral',    # 'lateral' | 'medial' | 'dorsal' | 'ventral'
    time_viewer=True,   # 时间滑块
    colorbar=True,
    clim=dict(kind='percent', lims=[90, 95, 99])  # 颜色范围
)

# 2. 自定义视角
brain = stc.plot(
    subjects_dir=subjects_dir,
    hemi='split',
    views=['lat', 'med', 'dor', 'ven'],  # 四视图
    time_unit='ms',
    size=(800, 800),
    background='white',
    foreground='black'
)

# 3. 动画
brain.save_movie(
    'stc_movie.mp4',
    time_dilation=20,   # 慢放
    tmin=0, tmax=0.5,
    framerate=30
)

# 4. 标签叠加
label = mne.read_label('lh.V1.label')
brain.add_label(label, color='green', alpha=0.7)

# 5. 峰值标注
brain.add_foci(
    stc.get_peak()[1],  # 峰值顶点
    coords_as_verts=True,
    hemi='lh',
    color='yellow',
    scale_factor=0.5
)

# 6. 截图
brain.save_image('brain_snapshot.png')
```

**交互功能** (鼠标):
- 左键拖动: 旋转视角
- 滚轮: 缩放
- 右键拖动: 平移
- 时间滑块: 动态播放激活

---

#### Brainstorm3: 解剖集成可视化

```matlab
% 双击源估计结果
view_surface_data(ResultsFile);

% 自动显示:
% - 3D皮层激活
% - 时间滑块
% - 脑区标注
% - MRI切片 (可选)

% GUI功能:
% - 旋转工具: 3D视角调整
% - 时间播放: 动画播放
% - 阈值滑块: 调整显示范围
% - 脑区Atlas: 点击显示时间序列
% - 平滑选项: 空间平滑强度
% - 颜色映射: 多种配色方案
% - 截图/录像: 高质量导出
```

**特色功能**:
- ✅ **MRI叠加**: 源激活+结构MRI同时显示
- ✅ **Atlas交互**: 点击脑区→显示ROI时间序列
- ✅ **多模态**: MEG源+fMRI激活融合显示

---

## 交互界面

### MNE-Python: 有限GUI工具

**可用GUI工具**:

1. **通道选择器** (pick_channels):
```python
picks = mne.pick_channels_regexp(raw.ch_names, regexp='MEG.*')
```

2. **事件编辑器** (少量交互):
```python
# 主要通过plot窗口交互
raw.plot(events=events)  # 可视化编辑事件
```

3. **坏通道标记** (plot交互):
```python
raw.plot()  # 点击通道→右键→标记为坏通道
# 或编程
raw.info['bads'] = ['MEG 0113']
```

4. **配准GUI** (mne.gui.coregistration):
```python
mne.gui.coregistration(
    subject='fsaverage',
    subjects_dir=subjects_dir
)
# 3D界面,调整传感器-头部配准
```

5. **Report查看器**:
```python
report = mne.Report()
report.add_raw(raw)
report.add_evoked(evoked)
report.save('report.html', open_browser=True)
# 生成HTML报告,浏览器查看
```

---

### Brainstorm3: 完整GUI生态

**GUI组件**:

1. **数据库浏览器** (左侧面板):
```
Protocol树形结构:
├── Subjects
│   ├── Subject001
│   │   ├── Anatomy (MRI, Surfaces)
│   │   └── Channel files
│   └── Subject002
└── Studies
    ├── @default_study
    │   ├── Data files
    │   ├── Results
    │   └── Timefreq
    └── Condition001
```

2. **进程管理器** (Process菜单):
```
分类清晰的处理函数:
├── Import data
├── Pre-process
│   ├── Band-pass filter
│   ├── Notch filter
│   └── Resample
├── Artifacts
│   ├── ICA components
│   ├── SSP
│   └── Detect artifacts
├── Sources
│   ├── Compute head model
│   ├── Compute sources
│   └── Project on cortex
└── Statistics
```

3. **脚本编辑器**:
```matlab
% 记录GUI操作为脚本
% 编辑器 > Script > Edit script pipeline
% 自动生成可重复脚本
```

4. **通道编辑器**:
```
图形界面调整:
- 电极位置 (拖拽)
- 通道类型 (下拉菜单)
- 坏通道标记 (点击)
- 传感器阵列可视化
```

5. **事件编辑器**:
```
时间序列上:
- 右键添加事件
- 拖动调整时间
- 双击编辑属性
- 自动检测节律
```

---

## 3D大脑可视化

### 渲染引擎对比

| 特性 | MNE-Python (PyVista) | Brainstorm3 (MATLAB) |
|------|---------------------|----------------------|
| **渲染引擎** | PyVista (VTK) | MATLAB Graphics |
| **交互性** | ⭐⭐⭐⭐ (鼠标操作) | ⭐⭐⭐⭐⭐ (GUI集成) |
| **性能** | ⭐⭐⭐⭐ (GPU加速) | ⭐⭐⭐ (CPU主导) |
| **自定义** | ⭐⭐⭐⭐⭐ (完全控制) | ⭐⭐⭐ (GUI参数) |
| **动画** | ⭐⭐⭐⭐ (MP4导出) | ⭐⭐⭐⭐ (AVI导出) |
| **截图质量** | ⭐⭐⭐⭐⭐ (高DPI) | ⭐⭐⭐⭐ (标准) |

---

### MNE-Python: 编程控制的3D

```python
# 完全自定义的大脑可视化
brain = stc.plot(
    subjects_dir=subjects_dir,
    hemi='both',
    views='lateral',
    background='white',
    foreground='black',
    colorbar=True,
    time_viewer=True,
    size=(800, 600),
    smoothing_steps=10,  # 平滑度
    alpha=1.0,           # 透明度
    cortex='classic',    # 皮层颜色方案
    clim=dict(kind='value', lims=[5, 10, 15])  # 绝对阈值
)

# 添加自定义元素
brain.add_text(0.05, 0.95, 'Peak at 100ms', 'title', font_size=14)
brain.add_annotation('aparc', borders=True, alpha=0.5)

# 多视图截图
for view in ['lat', 'med', 'dor', 'ven']:
    brain.show_view(view)
    brain.save_image(f'brain_{view}.png')

# 生成旋转动画
brain.save_movie(
    'rotation.mp4',
    time_dilation=20,
    tmin=0, tmax=0.5,
    framerate=24,
    time_viewer=True
)
```

---

### Brainstorm3: 集成的3D环境

```
GUI操作:
1. 双击源估计结果 → 自动3D显示
2. 工具栏:
   - 旋转工具
   - 缩放工具
   - 视角预设 (左/右/上/下)
   - 截图 (高分辨率)
   - 录像 (AVI)
3. 右侧面板:
   - 时间滑块 (播放动画)
   - 阈值滑块 (调整显示范围)
   - 色图选择 (Jet, Hot, Cool, ...)
   - 平滑选项
   - Atlas叠加 (Desikan, Destrieux, ...)
4. 交互:
   - 点击脑区 → 显示时间序列
   - Shift+点击 → 提取ROI
   - 右键 → 上下文菜单
```

**独特功能**:
- ✅ **MRI切片叠加**: 3D源+2D MRI切片同时显示
- ✅ **多模态融合**: MEG源+fMRI激活融合
- ✅ **脑区统计**: 自动计算每个Atlas区域的激活强度

---

## 报告生成

### MNE-Python: mne.Report

```python
from mne import Report

# 创建报告
report = Report(
    title='Subject 001 Analysis',
    subject='sub-001',
    subjects_dir=subjects_dir,
    verbose=True
)

# 添加内容
report.add_raw(raw, title='Raw Data', psd=True)
report.add_events(events, title='Events')
report.add_epochs(epochs, title='Epochs')
report.add_evoked(evoked, titles='Evoked')
report.add_stc(stc, title='Source Estimate', subject='fsaverage')

# 添加自定义图形
import matplotlib.pyplot as plt
fig, ax = plt.subplots()
ax.plot([1, 2, 3], [4, 5, 6])
report.add_figure(fig, title='Custom Plot')

# 添加代码
report.add_code('analysis_script.py', title='Analysis Code')

# 生成HTML
report.save('report.html', overwrite=True, open_browser=True)

# 生成HDF5 (可编辑)
report.save('report.hdf5', overwrite=True)
```

**生成的报告**:
- ✅ 单个HTML文件 (自包含)
- ✅ 交互式图形 (JavaScript)
- ✅ 可搜索目录
- ✅ 响应式布局 (手机/平板友好)

---

### Brainstorm3: 自动报告

```matlab
% 导出处理结果
bst_report('Start', 'MyReport');

% 添加图形 (自动截图)
bst_report('Snapshot', 'timeseries', DataFile);
bst_report('Snapshot', 'sources3d', ResultsFile);

% 生成报告
ReportFile = bst_report('Save', OutputFolder);
bst_report('Export', ReportFile, 'HTML');

% 包含:
% - 所有操作历史
% - 参数设置
% - 截图
% - 统计结果
```

---

## 功能对比总结

| 功能 | MNE-Python | Brainstorm3 | 优势 |
|------|------------|-------------|------|
| **时间序列图** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 🏆 Brainstorm (联动) |
| **拓扑图** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 🏆 MNE (自定义) |
| **3D大脑** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 🏆 Brainstorm (GUI) |
| **交互性** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 🏆 Brainstorm |
| **发表级图形** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 🏆 MNE (Matplotlib) |
| **批量绘图** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 🏆 MNE (脚本) |
| **报告生成** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⚖️ 平手 |
| **学习曲线** | ⭐⭐ | ⭐⭐⭐⭐⭐ | 🏆 Brainstorm |

---

## 各自优势

### MNE-Python 可视化优势

1. ✅ **Matplotlib集成**: 完全控制每个像素
2. ✅ **批量生成**: 循环创建100张图
3. ✅ **发表级质量**: 300+ DPI,矢量格式
4. ✅ **自定义**: 无限灵活性
5. ✅ **Python生态**: Seaborn, Plotly集成

```python
# 示例: 批量生成受试者报告
for subject in subjects:
    report = mne.Report(title=f'Subject {subject}')
    # ... 添加内容
    report.save(f'{subject}_report.html')
```

---

### Brainstorm3 可视化优势

1. ✅ **零编程**: 双击即可视化
2. ✅ **实时交互**: 调整参数立即生效
3. ✅ **联动视图**: 多窗口自动同步
4. ✅ **解剖集成**: MRI+Atlas+源激活一体化
5. ✅ **教学友好**: 概念可视化

---

## 选择建议

**选择 MNE-Python** 如果:
- 需要 **发表级图形** (Nature, Science)
- 需要 **批量生成** (100+ 受试者)
- 需要 **自定义样式** (配色,布局)
- 熟悉 **Matplotlib**

**选择 Brainstorm3** 如果:
- **探索性分析** (快速查看)
- **教学演示** (实时交互)
- **无编程经验**
- 需要 **Atlas交互** (脑区标注)

**混合使用**:
```
Brainstorm探索 → 确定参数 → MNE批量生成发表图
```

两者都能生成高质量可视化,只是方式不同! 🎨
